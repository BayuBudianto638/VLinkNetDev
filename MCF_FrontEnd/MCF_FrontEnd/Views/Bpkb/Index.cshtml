@model IEnumerable<MCF_FrontEnd.Models.Bpkb.BpkbModel>

@{
    ViewBag.Title = "BPKB";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">BPKB</h1>
    </div>
</div>

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                BPKB
            </div>

            <div class="panel-body">
                <div class="dataTable_wrapper">
                    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <thead>
                            <tr>
                                <th>Title</th>
                                <th>Description</th>
                                <th>Prices</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var mst in Model)
                            {
                                <tr class="odd gradeX">
                                    <td>
                                        @Html.DisplayFor(modelItem => mst.AgreementNumber)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => mst.Bpkb_No)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => mst.Police_No)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Edit", "Edit", new { id = mst.AgreementNumber }, new { @class = "btn btn-secondary btn-sm" }) |
                                        @Html.ActionLink("Details", "Details", new { id = mst.AgreementNumber }, new { @class = "btn btn-info btn-sm" })|
                                        @Html.ActionLink("Delete", "Delete", new { id = mst.AgreementNumber }, new { @class = "btn btn-danger btn-sm" })
                                    </td>
                                </tr>

                            }
                        </tbody>
                    </table>
                </div>
                <p>
                    <button class="btn btn-primary" onclick="location.href='@Url.Action("Create", "Beverage")';">Create</button>
                </p>
            </div>
        </div>
    </div>
</div>

